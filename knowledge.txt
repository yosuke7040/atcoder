Pythonで割り算を行う際に演算子 / を使うと、計算結果が小数型(float 型)になってしまいます。
整数値を出力したい場合は、演算子 // を使いましょう。

max(a, b, c, d)みたいに何個でもmax探してくれる

[2-10]
Python の場合文字列の一部の文字を単純に変更することはできません。
そのため、文字列を配列型に変換してから要素を入れ替え、文字列型に戻す必要があります。
S = "algo" 
S[0] = "b" # エラーが発生する

pythonの再起関数はデフォで1000回しか回せないので増やす
import sys
sys.setrecursionlimit(10**6)

pythonでインクリメント使えないじゃん